<div id="content-top">
  <% if (session.user) { %>
    <button id="checkin" class="checkin_out">LÅN UT</button>
    <button id="checkout" class="checkin_out">LEVER INN</button>
    <h3>Hei <%= session.user.AE %>!</h3>
    <p> epost: <%= session.user.BE %> </p>
    <p> meld: <%= session.user.AF %> </p>
    <button id="logout" type="submit">logg ut</button><br/>
  <% } else { %>
    <table>
      <tr>
        <td>lånernummer</td>
        <td><input id="userid"></input></p></td>
      </tr>
      <tr>
        <td>passord</td>
        <td><input id="pass"></input></p></td>
        <td><button id="login" type="submit">logg inn</button></td>
      </tr>
    </table>
  <% } %>
</div>

<% if (session.user) { %>
  <div id="content-middle">
    <table id="borrowtable" >
      <thead>
        <tr>
          <th>strekkode</th>
          <th>tittel</th>
          <th>lånt tom</th>
        </tr>
      </thead>
      <tbody>

      </tbody>
    </table>
  </div>
<% } %>
